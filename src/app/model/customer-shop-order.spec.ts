import { CustomerShopOrder } from './customer-shop-order';

describe('CustomerShopOrder', () => {
  it('should create an instance', () => {
    expect(new CustomerShopOrder()).toBeTruthy();
  });
});
